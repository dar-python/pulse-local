<div class="mc {{ $metric['tone'] }}">
    <div class="mc-label">{{ $metric['label'] }}</div>
    <div class="mc-value">{{ $metric['value'] }}</div>
    <div class="mc-note">{{ $metric['note'] }}</div>
</div>
